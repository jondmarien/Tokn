/**
 * Where the project lives.
 *
 * One place, because these end up in the footer, on the about page and in
 * metadata, and a stale link in one of three spots is the usual outcome of
 * typing them out each time.
 */

export const SOURCE_URL = "https://github.com/toknlabs/Tokn";
export const X_URL = "https://x.com/trytokn";
export const X_HANDLE = "@trytokn";

/**
 * Where problems go.
 *
 * The terms and the privacy policy both promise that questions and data
 * requests are handled by opening an issue, so this has to land on the issue
 * tracker rather than an organisation page that has none.
 */
export const ISSUES_URL = `${SOURCE_URL}/issues`;
